# NovelWiki AI plugin

Version 1.0.1. This plugin is copied into a private, single-run workspace. It has no
database, MCP, network, terminal, or secret-bearing integration. The agent proposes
artifacts under `output/`; NovelWiki validates and commits them.
